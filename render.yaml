services:
  - type: web
    name: superconnector-v5
    runtime: node
    buildCommand: npm install
    startCommand: node server-v5.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: WHATSAPP_VERIFY_TOKEN
        value: verify_token_123456
