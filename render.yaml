services:
  - type: web
    name: superconnector-v2
    runtime: node
    buildCommand: npm install
    startCommand: npx convex deploy --typecheck=disable && node server.js
    envVars:
      - key: NODE_ENV
        value: production
